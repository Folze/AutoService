$(document).ready(function () {
    $('.swiper').slick({
        adaptiveHeight: true,
        speed: 1000,
        prevArrow: '.btnLeft',
        nextArrow: '.btnRight',
    });
})



$(document).ready(function () {
    $('.swiperComment').slick({
        adaptiveHeight: true,
        speed: 1000,
        prevArrow: '.btnArrowComments',
        nextArrow: '.btnArrowComments2',
    });
})